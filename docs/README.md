# Documentation

This directory has the documentation for `door-opener`.

## Getting started

First, [view the README in `hardware/`](./hardware/README.md) in order to build
`door-opener`. Then, build and configure `door-opener` by following the
instructions [available in `software/`](./software/README.md)